name: Taito X Synthesis

on:
  push:
    paths:
      - 'chips/taito_x/**'
      - '.github/workflows/taito_x_synthesis.yml'
    branches:
      - master
      - main
      - sim-batch2
  pull_request:
    paths:
      - 'chips/taito_x/**'
  workflow_dispatch:

jobs:
  quartus-x-synthesis:
    runs-on: ubuntu-22.04
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4

      - name: Run Quartus synthesis (Docker)
        run: |
          set +e
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            raetro/quartus:17.0 \
            bash -c "cd /workspace/chips/taito_x/quartus && /opt/intelFPGA/quartus/bin/quartus_sh --flow compile taito_x 2>&1 | tee /workspace/taito_x_synthesis.log; exit \${PIPESTATUS[0]}"
          QEXIT=$?
          echo "Quartus exit code: $QEXIT"
          if [ $QEXIT -le 2 ]; then
            echo "Synthesis OK (0=clean 2=timing-only, both acceptable)"
            exit 0
          else
            echo "Synthesis FAILED (hard error)"
            exit 1
          fi

      - name: Synthesis report
        if: always()
        run: |
          echo "=== Resource usage ==="
          grep -E "Logic utilization|Total logic|M10K|MLAB|Fitter Status|Worst-case setup|Worst-case hold" taito_x_synthesis.log || true

      - name: Upload synthesis log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taito_x_synthesis_log
          path: taito_x_synthesis.log
          retention-days: 7

      - name: Collect artifacts
        if: success()
        run: |
          mkdir -p /tmp/x_artifacts
          cp chips/taito_x/quartus/output_files/taito_x.rbf /tmp/x_artifacts/
          cp chips/taito_x/mra/*.mra /tmp/x_artifacts/ 2>/dev/null || true
          ls -lh /tmp/x_artifacts/

      - name: Upload RBF
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: taito_x_rbf
          path: /tmp/x_artifacts/
          retention-days: 30
          compression-level: 0

      - name: Upload synthesis reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taito_x_synthesis_reports
          path: |
            chips/taito_x/quartus/output_files/*.rpt
            chips/taito_x/quartus/*.rpt
          retention-days: 14

      - name: Report status
        if: always()
        run: |
          echo "Job Status: ${{ job.status }}"
          RPT=chips/taito_x/quartus/output_files/taito_x.fit.rpt
          if [ -f "$RPT" ]; then
            grep -E "Logic utilization|Total registers|Total pins|Total block memory|Total RAM Blocks" "$RPT" || true
          fi
