name: Taito X Synthesis

on:
  push:
    paths:
      - 'chips/taito_x/**'
      - '.github/workflows/taito_x_synthesis.yml'
    branches:
      - master
      - main
  pull_request:
    paths:
      - 'chips/taito_x/**'
  workflow_dispatch:

jobs:
  quartus-x-synthesis:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4

      - name: Run Quartus synthesis
        run: |
          mkdir -p /tmp/quartus_diag
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/workspace" \
            raetro/quartus:17.0 \
            sh -c "cd /workspace && quartus_sh --flow compile chips/taito_x/quartus/taito_x" 2>&1 | tee /tmp/quartus_diag/x_synthesis.log
          EXIT=${PIPESTATUS[0]}
          echo "Quartus exit code: $EXIT"
          exit $EXIT

      - name: Upload synthesis log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taito_x_synthesis_log
          path: /tmp/quartus_diag/x_synthesis.log
          retention-days: 7

      - name: Collect artifacts
        if: success()
        run: |
          mkdir -p /tmp/x_artifacts
          cp chips/taito_x/quartus/output_files/taito_x.rbf /tmp/x_artifacts/
          cp chips/taito_x/mra/*.mra /tmp/x_artifacts/ 2>/dev/null || true
          ls -lh /tmp/x_artifacts/

      - name: Upload RBF
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: taito_x_rbf
          path: /tmp/x_artifacts/
          retention-days: 30
          compression-level: 0

      - name: Upload synthesis reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taito_x_synthesis_reports
          path: |
            chips/taito_x/quartus/output_files/*.rpt
            chips/taito_x/quartus/*.rpt
          retention-days: 14

      - name: Report status
        if: always()
        run: |
          echo "Job Status: ${{ job.status }}"
          RPT=chips/taito_x/quartus/output_files/taito_x.fit.rpt
          if [ -f "$RPT" ]; then
            grep -E "^Total logic elements|Total memory bits|Total pins|I/O pins used" "$RPT" || true
          fi
