name: Taito F3 Synthesis

on:
  push:
    paths:
      - 'chips/taito_f3/**'
      - '.github/workflows/taito_f3_synthesis.yml'
    branches:
      - master
      - main
  pull_request:
    paths:
      - 'chips/taito_f3/**'
  workflow_dispatch:

jobs:
  quartus-f3-synthesis:
    runs-on: ubuntu-22.04
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4

      - name: Run Quartus synthesis (Docker)
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            raetro/quartus:17.0 \
            sh -c "cd /workspace/chips/taito_f3/quartus && /opt/intelFPGA/quartus/bin/quartus_sh --flow compile taito_f3 2>&1 | tee /workspace/taito_f3_synthesis.log; exit \${PIPESTATUS[0]}"

      - name: Upload synthesis log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taito_f3_synthesis_log
          path: taito_f3_synthesis.log
          retention-days: 7

      - name: Collect artifacts
        if: success()
        run: |
          mkdir -p /tmp/f3_artifacts
          cp chips/taito_f3/quartus/output_files/taito_f3.rbf /tmp/f3_artifacts/
          cp chips/taito_f3/mra/*.mra /tmp/f3_artifacts/ 2>/dev/null || true
          ls -lh /tmp/f3_artifacts/

      - name: Upload RBF
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: taito_f3_rbf
          path: /tmp/f3_artifacts/
          retention-days: 30
          compression-level: 0

      - name: Upload synthesis reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: taito_f3_synthesis_reports
          path: |
            chips/taito_f3/quartus/output_files/*.rpt
            chips/taito_f3/quartus/*.rpt
          retention-days: 14

      - name: Report status
        if: always()
        run: |
          echo "Job Status: ${{ job.status }}"
          RPT=chips/taito_f3/quartus/output_files/taito_f3.fit.rpt
          if [ -f "$RPT" ]; then
            grep -E "Logic utilization|Total registers|Total pins|Total block memory|Total RAM Blocks" "$RPT" || true
          fi
